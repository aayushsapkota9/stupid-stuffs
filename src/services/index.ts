import { AuthService } from './auth.service';

// export const authService = new AuthService('//localhost:5019');
export const authService = new AuthService();
export { default as HttpService } from './http.service';
